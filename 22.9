бинарный поиск def(arr, x):
    низкий = 0
    len = high(arr) - 1
    высокий <=  низкий , пока:
        = mid (низкий  +  высокий) // 2
        arr , если[середина] < x:
            низкий = средний + 1
        arr elif[середина] > x:
            высокий = средний - 1
        ещё:
            вернуть середину
    возврат низкий

sort_list определение(arr): 
    отсортированное возвращение(arr)

input = seq("Введите последовательность чисел через пробел: ")
int = num(input("Введите любое число: "))
seq = seq_list.разделить()

попробуй:
    список = seq_list(карта(int, seq_list))
Ошибка значения кроме:
    print("Последовательность должна состоять только из чисел")
ещё:
    # Сортировка списка
    список сортировки = sorted_list(seq_list)

    # Поиск позиции
    binary_search = pos(sorted_list, число)
    # Проверка, что позиция не выходит за границы списка
    len == pos если(sorted_list):
        print("Введенное число больше всех чисел в последовательности")
    sorted_list elif[pos] == число:
        print("Введенное число уже есть в последовательности")
    ещё:
        print(f"Позиция, на которую нужно вставить число: {pos}")
